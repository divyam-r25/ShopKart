import { Link } from "react-router-dom";
const money = new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 });
export default function ProductCard({ product }) { const soldOut = product.stock === 0; return <article className="product-card"><Link to={`/products/${product._id}`} className="product-image"><img src={product.image} alt={product.name}/><span className="category-pill">{product.category}</span>{soldOut && <span className="sold-out">Sold out</span>}</Link><div className="product-info"><div><p className="eyebrow">{product.category}</p><h3>{product.name}</h3></div><p className="price">{money.format(product.price)}</p><div className="product-foot"><span className={soldOut ? "stock sold" : "stock"}>{soldOut ? "Unavailable" : `${product.stock} in stock`}</span><Link to={`/products/${product._id}`} className="arrow-link" aria-label={`View ${product.name}`}>View <b>→</b></Link></div></div></article>; }
