import { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";
import Navbar from "../components/Navbar";
import { getProduct } from "../services/api";
const money = new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 });
export default function ProductDetails({ user }) { const { id } = useParams(); const [product, setProduct] = useState(null); const [error, setError] = useState(false); const [added, setAdded] = useState(false); useEffect(() => { getProduct(id).then(({data}) => setProduct(data.product)).catch(() => setError(true)); }, [id]); if (error) return <><Navbar user={user}/><div className="state detail-state"><h3>We couldn’t find that product.</h3><Link className="text-link" to="/products">← Back to the shop</Link></div></>; if (!product) return <><Navbar user={user}/><div className="state detail-state"><div className="spinner"/><p>Opening the details…</p></div></>; return <><Navbar user={user}/><main className="details"><Link to="/products" className="back">← Back to the collection</Link><section className="detail-grid"><div className="detail-image"><img src={product.image} alt={product.name}/><span>{product.category}</span></div><div className="detail-copy"><p className="eyebrow accent">{product.category}</p><h1>{product.name}</h1><p className="detail-price">{money.format(product.price)}</p><div className="rule"/><p className="description">{product.description}</p><p className={product.stock ? "availability" : "availability no-stock"}>{product.stock ? `${product.stock} pieces currently available` : "Currently unavailable"}</p><button className="primary-btn add-cart" disabled={!product.stock} onClick={() => setAdded(true)}>{added ? "Added to your wishlist ✓" : product.stock ? "Add to cart  +" : "Out of stock"}</button><p className="fine-print">Complimentary delivery on orders over ₹1,500.</p></div></section></main></>; }
